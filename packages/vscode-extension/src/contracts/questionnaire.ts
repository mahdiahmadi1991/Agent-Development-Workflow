export interface GroupASelections {
  gitMode: "track" | "ignore";
}

export interface QuestionnaireAssetLoad {
  family: string;
  indexPath: string;
  flowPath: string;
  indexRaw: string;
  flowRaw: string;
}
