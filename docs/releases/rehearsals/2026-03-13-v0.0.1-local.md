# Local Release Rehearsal - v0.0.1

Date: 2026-03-13
Operator: Codex
Mode: local pre-publish rehearsal

## Commands Executed
- `scripts/rehearse-release-local.sh 0.0.1`

## Stage Results
1. Release docs validation: pass
2. Package version validation: pass
3. Dependency install (`npm ci`): pass
4. Test suite (`npm test`): pass (62/62)
5. Compile (`npm run compile`): pass
6. VSIX package (`npm run package:ci`): pass
7. Checksum generation (`sha256sum`): pass
8. Checksum verification (`sha256sum -c`): pass

## Generated Artifacts
- `packages/vscode-extension/artifacts/codex-onboarding.vsix`
- `packages/vscode-extension/artifacts/codex-onboarding.vsix.sha256`

## Notes
- Rehearsal confirms local release pipeline readiness for `v0.0.1`.
- GitHub-hosted workflow execution is not validated by this local rehearsal and remains part of release-day checks.
